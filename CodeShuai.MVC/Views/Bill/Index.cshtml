@{
    ViewData["Title"] = "Bill Page";
}

<!-- 开发环境版本，包含了有帮助的命令行警告 -->
<div id="app">

    <cs-bill v-for="post in obj" v-bind:key="post.id" v-bind:post="post"></cs-bill>

    <button class="btn btn-primary" v-on:click="val">dian</button>

    <nav id="sjnavFoot" class="navbar fixed-bottom justify-content-center navbar-light" style="background-color: #e3f2fd;">
        <a class="nav-link" href="#">Link</a>
        <a class="nav-link " href="#"><i class="fa fa-plus" style="font-size:30px;"></i></a>
        <a class="nav-link" href="#">Link</a>
    </nav>
</div>
<script>

    //var sjnavHeadHeight = $("#sjnavHead").outerHeight();
    var sjnavFootHeight = $("#sjnavFoot").outerHeight();
    $("#jsMain").css({  "margin-bottom": sjnavFootHeight + 10  })

    var data = { message: "Hello Vue!", obj: "" };
    var app = new Vue({
        el: '#app',
        data: data,
        methods: {
            val: function () {
                $.ajax({
                    url: "/api/values/getbills?id=1",
                    date: "",
                    dateType: "json",
                    type: "get"
                }).done(function (ret) {
                    console.log(ret);
                    data.obj = JSON.parse(JSON.stringify(ret));
                    //JSON.parse(data.obj);
                })
            }
        }
    })

</script>
